# 2-node SONiC VRF spike (issue #6) — link in VRF red with per-VRF OSPF.
provider: clab
plugin: [ multilab ]
defaults:
  device: sonic_clab
  multilab:
    id: 52
module: [ vrf, ospf ]
vrfs:
  red:
nodes: [ s1, s2 ]
links:
- s1:
  s2:
  vrf: red
